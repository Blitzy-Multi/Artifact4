# Runtime dependencies for the Artifact4 Flask server.
# Versions pinned per Technical Specification / Agent Action Plan (AAP) §0.3.1.
# Python 3.13 runtime (AAP recommended 3.12.x; 3.13 used as it is the host
# toolchain and is fully supported by Flask 3.1.x).

Flask==3.1.3
python-dotenv==1.2.2
gunicorn==26.0.0
Werkzeug==3.1.8
