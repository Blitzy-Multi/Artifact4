# Development / test dependencies for the Artifact4 Flask server.
#
# This file layers the development and test tooling on top of the baseline
# runtime stack. The `-r requirements.txt` directive below includes every
# runtime dependency, so a single `pip install -r requirements-dev.txt`
# provisions one environment that can both run AND test the application.
#
# Per the Agent Action Plan (AAP §0.3.2), the Python test runner `pytest`
# replaces the original Node.js project's dev/test tooling (e.g. jest / mocha).
# It is the sole baseline development dependency (AAP §0.5.1 Group 2, §0.3.1).
#
# Versioning policy (AAP §0.7): EXACT-pinned with `==` only — no ranges,
# no "latest", no placeholders. The pin below was verified against PyPI
# (2026-05-29) and is confirmed installable on the project toolchain
# (Python 3.13; Flask 3.1.x).
#
# Scope note: the baseline intentionally declares ONLY pytest. Additional dev
# tools (coverage, linters, etc.) are NOT added here. Conditional test/runtime
# packages are deferred until the original Node.js source is supplied and the
# corresponding capability is confirmed, then pinned per AAP §0.3.1 / §0.3.2.

# Baseline runtime dependencies (Flask, python-dotenv, gunicorn, Werkzeug).
-r requirements.txt

# Test runner (replaces Node jest/mocha) — AAP §0.3.1 / §0.3.2.
pytest==9.0.3
